#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):

    for row in matrix:
        for column in row:
            matrix != (len(matrix[column]), (-1))
            print("{:d}".format(column), end=" ")
        print()
